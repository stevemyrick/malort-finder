# Malört Finder

A simple package for searching the Wake County ABC for Malört using Selenium.

## Usage

Install dependencies with `python -m pip install -r requirements.txt`.

Run `python search.py` to test the API.

TODO: Set up website frontend to query the API with user's location (or address from a textbox) and display results on an Open Street Map. Use the cache functionality to limits hits to the inventory website. Add pins for each location, quantity, and price where its available. Default the location to Cackalackycon. On the pins, provide links to open in navigation apps etc..